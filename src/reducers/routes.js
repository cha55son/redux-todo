import { UPDATE_PATH } from 'redux-simple-router'

export function reduceRoutes(state = {}, action) {
    switch(action.type) {
        case UPDATE_PATH:
            let noop
        default:
            return state
    }
}
